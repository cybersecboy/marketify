import React from 'react';

const Home = () => {
    // const slice1 = useSelector(store => store.slice1.items)
    // console.log(import.meta.env.VITE_MYKEY);

    return (
        <>
            <main>
                <h1>main</h1>
            </main>
        </>
    )
}

export default Home;
